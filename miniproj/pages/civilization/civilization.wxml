<!--index.wxml-->
<view class="container">

    <view class='section' wx:for="{{nodes}}" wx:key = "id">
      <text>{{item.ddate}}</text>
      <text bindtap='go' data-id='{{item.id}}'>{{item.title}}</text>
    </view>

</view>
